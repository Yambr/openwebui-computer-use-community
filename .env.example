# AI Computer Use - Environment Variables
# Copy this file to .env and customize as needed

# Python settings
PYTHONUNBUFFERED=1
PIP_BREAK_SYSTEM_PACKAGES=1

# Node.js settings
NODE_PATH=/usr/local/lib/node_modules_global
NODE_EXTRA_CA_CERTS=/etc/ssl/certs/ca-certificates.crt

# Java settings
JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64

# Playwright settings
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers

# SSL Certificate settings
REQUESTS_CA_BUNDLE=/etc/ssl/certs/ca-certificates.crt
SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt

# Network proxy settings (uncomment if needed)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1,169.254.169.254

# Resource limits (modify in docker-compose.yml)
# CPU_LIMIT=4.0
# MEMORY_LIMIT=8G

# Optional: Custom paths
# CUSTOM_SKILLS_PATH=/path/to/custom/skills

# Claude Code / Anthropic API configuration
# Get your API key from https://console.anthropic.com/
#
# ANTHROPIC_BASE_URL can point to:
# - https://api.anthropic.com (direct Anthropic API)
# - http://localhost:4000 (LiteLLM proxy)
# - Your custom API gateway
ANTHROPIC_BASE_URL=https://api.anthropic.com
ANTHROPIC_API_KEY=your-anthropic-api-key

# Optional: Custom headers for API requests
# ANTHROPIC_CUSTOM_HEADERS="X-Custom-Header: value"

# File Server MCP configuration
# API key for MCP endpoint authorization (Authorization: Bearer <key>)
MCP_API_KEY=your-secure-mcp-api-key

# GitLab configuration (optional)
# GITLAB_HOST=gitlab.com
# GITLAB_TOKEN=your-gitlab-token
